{
  "name": "tower-redesigned",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:server",
    "setup:app": "npm run setup:server && npm run setup:client",
    "setup:client": "cd Tower-Redesigned.client && npm i",
    "setup:server": "cd Tower-Redesigned && npm i",
    "start": "node Tower-Redesigned/index.js"
  },
  "engines": {
    "node": "^14.17.0"
  }
}
